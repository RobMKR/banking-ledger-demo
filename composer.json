{
    "name": "robert/ledger",
    "description": "In-memory, append-only account ledger core",
    "type": "project",
    "license": "proprietary",
    "require": {
        "php": "^8.4"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0"
    },
    "autoload": {
        "psr-4": {
            "Ledger\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Ledger\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "test:golden": "phpunit tests/Golden --testdox",
        "test:known-failure": "phpunit --group known-failure",
        "replay": "php bin/replay"
    },
    "config": {
        "sort-packages": true
    },
    "scripts-descriptions": {
        "test": "The full suite. Must be green. Excludes the deliberate failure.",
        "test:golden": "The golden test alone: the shipped configuration locked end to end.",
        "test:known-failure": "The one test committed failing, on purpose. Expected to fail.",
        "replay": "Replay the assessment stream and print the per-day report."
    }
}
